package com.crowdproj.teams.back.transport.rest.common.models

enum class TeamOperations {
    APPLY,
    UNAPPLY,
    JOIN,
    LEAVE,
    UPDATE,
    INVITE,
    DENY_INVITATION,
    ACCEPT_INVITATION
}